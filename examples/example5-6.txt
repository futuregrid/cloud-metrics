# New metric for system utilization
# These commands will produce a line chart for system usage
clear all

# A user can specify a data range
set search_range 2011-11-01T00:00:00 2011-11-30T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S ccvm_cores

# Finally, it can create a graph for the analyzed data
sys_report -o 2011-11/india/cores/

analyze -S ccvm_mem

sys_report -o 2011-11/india/mem

analyze -S ccvm_disk

sys_report -o 2011-11/india/disk

clear all

# A user can specify a data range
set search_range 2011-12-01T00:00:00 2011-12-31T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S ccvm_cores

# Finally, it can create a graph for the analyzed data
sys_report -o 2011-12/india/cores/

analyze -S ccvm_mem

sys_report -o 2011-12/india/mem

analyze -S ccvm_disk

sys_report -o 2011-12/india/disk

clear all

# A user can specify a data range
set search_range 2012-01-01T00:00:00 2012-01-31T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S ccvm_cores

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-01/india/cores/

analyze -S ccvm_mem

sys_report -o 2012-01/india/mem

analyze -S ccvm_disk

sys_report -o 2012-01/india/disk

clear all

# A user can specify a data range
set search_range 2012-02-01T00:00:00 2012-02-28T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S ccvm_cores

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-02/india/cores/

analyze -S ccvm_mem

sys_report -o 2012-02/india/mem

analyze -S ccvm_disk

sys_report -o 2012-02/india/disk

clear all

# A user can specify a data range
set search_range 2012-03-01T00:00:00 2012-03-31T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S ccvm_cores

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-03/india/cores/

analyze -S ccvm_mem

sys_report -o 2012-03/india/mem

analyze -S ccvm_disk

sys_report -o 2012-03/india/disk

clear all

# A user can specify a data range
set search_range 2012-04-01T00:00:00 2012-04-30T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S ccvm_cores

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-04/india/cores/

analyze -S ccvm_mem

sys_report -o 2012-04/india/mem

analyze -S ccvm_disk

sys_report -o 2012-04/india/disk

clear all

# A user can specify a data range
set search_range 2012-05-01T00:00:00 2012-05-31T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S ccvm_cores

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-05/india/cores/

analyze -S ccvm_mem

sys_report -o 2012-05/india/mem

analyze -S ccvm_disk

sys_report -o 2012-05/india/disk

clear all

# A user can specify a data range
set search_range 2012-05-01T00:00:00 2012-05-31T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename sierra

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S ccvm_cores

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-05/sierra/cores/

analyze -S ccvm_mem

sys_report -o 2012-05/sierra/mem

analyze -S ccvm_disk

sys_report -o 2012-05/sierra/disk


