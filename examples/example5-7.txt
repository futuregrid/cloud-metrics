# New metric for system utilization
# These commands will produce a line chart for system usage
clear all

# A user can specify a data range
set search_range 2011-11-01T00:00:00 2011-11-30T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S count

# Finally, it can create a graph for the analyzed data
sys_report -o 2011-11/india/eucalyptus/count --all

clear all

# A user can specify a data range
set search_range 2011-11-01T00:00:00 2011-11-30T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S runtime

# Finally, it can create a graph for the analyzed data
sys_report -o 2011-11/india/eucalyptus/runtime --all

clear all

# A user can specify a data range
set search_range 2011-12-01T00:00:00 2011-12-31T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S count

# Finally, it can create a graph for the analyzed data
sys_report -o 2011-12/india/eucalyptus/count --all

clear all

# A user can specify a data range
set search_range 2011-12-01T00:00:00 2011-12-31T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S runtime

# Finally, it can create a graph for the analyzed data
sys_report -o 2011-12/india/eucalyptus/runtime --all

clear all

# A user can specify a data range
set search_range 2012-01-01T00:00:00 2012-01-31T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S count

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-01/india/eucalyptus/count --all

clear all

# A user can specify a data range
set search_range 2012-01-01T00:00:00 2012-01-31T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S runtime

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-01/india/eucalyptus/runtime --all

clear all

# A user can specify a data range
set search_range 2012-02-01T00:00:00 2012-02-28T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S count

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-02/india/eucalyptus/count --all

clear all

# A user can specify a data range
set search_range 2012-02-01T00:00:00 2012-02-28T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S runtime

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-02/india/eucalyptus/runtime --all

clear all

# A user can specify a data range
set search_range 2012-03-01T00:00:00 2012-03-31T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S count

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-03/india/eucalyptus/count --all

clear all

# A user can specify a data range
set search_range 2012-03-01T00:00:00 2012-03-31T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S runtime

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-03/india/eucalyptus/runtime --all

clear all

# A user can specify a data range
set search_range 2012-04-01T00:00:00 2012-04-30T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S count

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-04/india/eucalyptus/count --all

clear all

# A user can specify a data range
set search_range 2012-04-01T00:00:00 2012-04-30T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S runtime

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-04/india/eucalyptus/runtime --all

clear all

# A user can specify a data range
set search_range 2012-05-01T00:00:00 2012-05-31T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S count

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-05/india/eucalyptus/count --all

clear all

# A user can specify a data range
set search_range 2012-05-01T00:00:00 2012-05-31T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S runtime

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-05/india/eucalyptus/runtime --all

clear all

# A user can specify a data range
set search_range 2012-05-01T00:00:00 2012-05-31T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename sierra

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S count

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-05/sierra/eucalyptus/count --all

clear all

# A user can specify a data range
set search_range 2012-05-01T00:00:00 2012-05-31T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename sierra

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S runtime

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-05/sierra/eucalyptus/runtime --all

clear all

# A user can specify a data range
set search_range 2012-06-01T00:00:00 2012-06-30T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S count

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-06/india/eucalyptus/count --all

clear all

# A user can specify a data range
set search_range 2012-06-01T00:00:00 2012-06-30T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S runtime

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-06/india/eucalyptus/runtime --all

clear all

# A user can specify a data range
set search_range 2012-06-01T00:00:00 2012-06-30T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename sierra

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S count

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-06/sierra/eucalyptus/count --all

clear all

# A user can specify a data range
set search_range 2012-06-01T00:00:00 2012-06-30T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename sierra

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S runtime

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-06/sierra/eucalyptus/runtime --all

clear all

# A user can specify a data range
set search_range 2012-07-01T00:00:00 2012-07-31T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S count

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-07/india/eucalyptus/count --all

clear all

# A user can specify a data range
set search_range 2012-07-01T00:00:00 2012-07-31T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename india

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S runtime

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-07/india/eucalyptus/runtime --all

clear all

# A user can specify a data range
set search_range 2012-07-01T00:00:00 2012-07-3T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename sierra

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S count

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-07/sierra/eucalyptus/count --all

clear all

# A user can specify a data range
set search_range 2012-07-01T00:00:00 2012-07-31T23:59:59

# A user can specify a node name (e.g. india, sierra)
set nodename sierra

set platform eucalyptus

# It requires a metric which can be specified with -S, --stat option with analyze command.
analyze -S runtime

# Finally, it can create a graph for the analyzed data
sys_report -o 2012-07/sierra/eucalyptus/runtime --all
